<?php

if(!defined('IN_DISCUZ')) {
	exit('Access Denied');
}

// $sql = <<<EOF

// CREATE TABLE IF NOT EXISTS `cdb_plugin_transaction` (
 // `id` int(8) NOT NULL AUTO_INCREMENT,
 // `tid` int(8) unsigned NOT NULL,
 // `uid` int(8) unsigned NOT NULL,
 // `price` int(8) unsigned NOT NULL,
 // `number` int(8) unsigned NOT NULL,
 // `wx` varchar(50) NOT NULL,
 // `zfb` varchar(50) NOT NULL,
 // `buyer` int(8) unsigned DEFAULT NULL,
 // `c_time` datetime NOT NULL,
 // `e_time` datetime DEFAULT NULL,
 // `b_time` datetime DEFAULT NULL,
 // `p_time` datetime DEFAULT NULL,
 // `f_time` datetime DEFAULT NULL,
 // `status` varchar(10) NOT NULL,
 // PRIMARY KEY (`id`)
// ) ENGINE=MyISAM AUTO_INCREMENT=18 DEFAULT CHARSET=utf8

// EOF;

// runquery($sql);

$finish = true;